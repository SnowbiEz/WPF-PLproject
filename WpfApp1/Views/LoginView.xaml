<UserControl x:Class="WpfApp1.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Right Panel for Login -->
        <ui:SimpleStackPanel  Grid.Column="1" VerticalAlignment="Center">
            <TextBox x:Name="txtUsername" Width="200" />
            <PasswordBox x:Name="txtPassword" Width="200"  />
            <!--<ui:AppBarButton x:Name="Login" Width="100" Content="Login" HorizontalAlignment="Center"/>-->
            <Button x:Name="btnSubmit" Width="100" Content="Login" HorizontalAlignment="Center"/>
            <Button x:Name="btnRegister" Width ="100" Content ="Register" HorizontalAlignment="Center" />
        </ui:SimpleStackPanel>
    </Grid>
</UserControl>
